<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.catchtable.clone.infrastructure.member.MemberStore">
    <insert id="store" parameterType="com.catchtable.clone.domain.member.Member"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tbl_members (member_token, name, phone, password, nickname, created_at)
        VALUES (#{memberToken}, #{name}, #{phone}, #{password}, #{nickname}, #{createdAt});
    </insert>
    <!--
    <insert id="store" parameterType="com.catchtable.clone.vo.UserVO">
        INSERT INTO tbl_terms (mno, peristalsis, terms_1, terms_2, terms_3, terms_4, terms_5, terms_6_1, terms_6_2,
        terms_6_3)
        VALUES (#{mno}, #{peristalsis}, #{terms_1}, #{terms_2}, #{terms_3}, #{terms_4}, #{terms_5}, #{terms_6_1},
        #{terms_6_2}, #{terms_6_3});
    </insert>
    <select id="nicknameCheck" parameterType="com.catchtable.clone.vo.UserVO">
        SELECT * FROM tbl_user WHERE nickname = #{nickname};
    </select>
    <select id="loginUser" parameterType="com.catchtable.clone.vo.UserVO">
        SELECT * FROM tbl_user WHERE phone = #{phone} OR nickname = #{nickname} AND password = #{password}
    </select>
    -->
</mapper>